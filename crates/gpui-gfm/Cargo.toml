[package]
name = "gpui-gfm"
version = "0.2.0"
edition.workspace = true

[lib]
path = "lib.rs"

[dependencies]
gpui.workspace = true
comrak.workspace = true
tl.workspace = true

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }
